/**
 * Module sitemap.js
 * Generate sitemap.xml and robots.txt files for client
 *
 * @author  Marunin Alexey <amarunin@oneplus1.ru>
 * @since   0.1.0
 */

/*
const sitemap = require('express-sitemap')

module.exports = function (app) {
  sitemap({
    http: 'https',
    url: 'charityblockchain.ru',
    sitemap: './client/dist/sitemap.xml',
    robots: './client/dist/robots.txt',
    map: {
      '/foundations': ['get'],
      '/association': ['get'],
    },
    route: {
      '/api': {
        disallow: true,
      },
    },
  }).toFile();
}
*/
